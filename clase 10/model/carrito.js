// aca solo estarán las correciones a la base de datos

export const getProductById = (id) => {
  if (!id) return null;

  const respuestaDeMongo = []; //mongodb.get.... id.
  return respuestaDeMongo;
};
